import { Layout } from 'src/components/layout';
import React from 'react';

type BlogScreenProps = {};

export const BlogScreen = ({}: BlogScreenProps) => {
  return <Layout>this is a blog!</Layout>;
};
